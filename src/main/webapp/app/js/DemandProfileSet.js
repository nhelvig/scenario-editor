// Generated by CoffeeScript 1.3.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  window.sirius.DemandProfileSet = (function(_super) {
    var $a;

    __extends(DemandProfileSet, _super);

    /* $a = alias for sirius namespace
    */


    function DemandProfileSet() {
      return DemandProfileSet.__super__.constructor.apply(this, arguments);
    }

    $a = window.sirius;

    DemandProfileSet.from_xml1 = function(xml, object_with_id) {
      var deferred, fn, obj, _i, _len;
      deferred = [];
      obj = this.from_xml2(xml, deferred, object_with_id);
      for (_i = 0, _len = deferred.length; _i < _len; _i++) {
        fn = deferred[_i];
        fn();
      }
      return obj;
    };

    DemandProfileSet.from_xml2 = function(xml, deferred, object_with_id) {
      var VehicleTypeOrder, demandProfile, description, id, name, obj;
      if (!(xml != null) || xml.length === 0) {
        return null;
      }
      obj = new window.sirius.DemandProfileSet();
      description = xml.children('description');
      obj.set('description', $a.Description.from_xml2(description, deferred, object_with_id));
      VehicleTypeOrder = xml.children('VehicleTypeOrder');
      obj.set('vehicletypeorder', $a.VehicleTypeOrder.from_xml2(VehicleTypeOrder, deferred, object_with_id));
      demandProfile = xml.children('demandProfile');
      obj.set('demandprofile', _.map($(demandProfile), function(demandProfile_i) {
        return $a.DemandProfile.from_xml2($(demandProfile_i), deferred, object_with_id);
      }));
      id = $(xml).attr('id');
      obj.set('id', id);
      name = $(xml).attr('name');
      obj.set('name', name);
      if (obj.resolve_references) {
        obj.resolve_references(deferred, object_with_id);
      }
      return obj;
    };

    DemandProfileSet.prototype.to_xml = function(doc) {
      var xml;
      xml = doc.createElement('DemandProfileSet');
      if (this.encode_references) {
        this.encode_references();
      }
      if (this.has('description')) {
        xml.appendChild(this.get('description').to_xml(doc));
      }
      if (this.has('vehicletypeorder')) {
        xml.appendChild(this.get('vehicletypeorder').to_xml(doc));
      }
      _.each(this.get('demandprofile') || [], function(a_demandprofile) {
        return xml.appendChild(a_demandprofile.to_xml(doc));
      });
      if (this.has('id') && this.id !== "") {
        xml.setAttribute('id', this.get('id'));
      }
      if (this.has('name') && this.name !== "") {
        xml.setAttribute('name', this.get('name'));
      }
      return xml;
    };

    DemandProfileSet.prototype.deep_copy = function() {
      return DemandProfileSet.from_xml1(this.to_xml(), {});
    };

    DemandProfileSet.prototype.inspect = function(depth, indent, orig_depth) {
      if (depth == null) {
        depth = 1;
      }
      if (indent == null) {
        indent = false;
      }
      if (orig_depth == null) {
        orig_depth = -1;
      }
      return null;
    };

    DemandProfileSet.prototype.make_tree = function() {
      return null;
    };

    return DemandProfileSet;

  })(Backbone.Model);

}).call(this);
