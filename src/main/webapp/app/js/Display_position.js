// Generated by CoffeeScript 1.3.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  window.sirius.Display_position = (function(_super) {
    var $a;

    __extends(Display_position, _super);

    /* $a = alias for sirius namespace
    */


    function Display_position() {
      return Display_position.__super__.constructor.apply(this, arguments);
    }

    $a = window.sirius;

    Display_position.from_xml1 = function(xml, object_with_id) {
      var deferred, fn, obj, _i, _len;
      deferred = [];
      obj = this.from_xml2(xml, deferred, object_with_id);
      for (_i = 0, _len = deferred.length; _i < _len; _i++) {
        fn = deferred[_i];
        fn();
      }
      return obj;
    };

    Display_position.from_xml2 = function(xml, deferred, object_with_id) {
      var obj, point;
      if (!(xml != null) || xml.length === 0) {
        return null;
      }
      obj = new window.sirius.Display_position();
      point = xml.children('point');
      obj.set('point', _.map($(point), function(point_i) {
        return $a.Point.from_xml2($(point_i), deferred, object_with_id);
      }));
      if (obj.resolve_references) {
        obj.resolve_references(deferred, object_with_id);
      }
      return obj;
    };

    Display_position.prototype.to_xml = function(doc) {
      var xml;
      xml = doc.createElement('display_position');
      if (this.encode_references) {
        this.encode_references();
      }
      _.each(this.get('point') || [], function(a_point) {
        return xml.appendChild(a_point.to_xml(doc));
      });
      return xml;
    };

    Display_position.prototype.deep_copy = function() {
      return Display_position.from_xml1(this.to_xml(), {});
    };

    Display_position.prototype.inspect = function(depth, indent, orig_depth) {
      if (depth == null) {
        depth = 1;
      }
      if (indent == null) {
        indent = false;
      }
      if (orig_depth == null) {
        orig_depth = -1;
      }
      return null;
    };

    Display_position.prototype.make_tree = function() {
      return null;
    };

    return Display_position;

  })(Backbone.Model);

}).call(this);
