// Generated by CoffeeScript 1.3.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  window.sirius.Inputs = (function(_super) {
    var $a;

    __extends(Inputs, _super);

    /* $a = alias for sirius namespace
    */


    function Inputs() {
      return Inputs.__super__.constructor.apply(this, arguments);
    }

    $a = window.sirius;

    Inputs.from_xml1 = function(xml, object_with_id) {
      var deferred, fn, obj, _i, _len;
      deferred = [];
      obj = this.from_xml2(xml, deferred, object_with_id);
      for (_i = 0, _len = deferred.length; _i < _len; _i++) {
        fn = deferred[_i];
        fn();
      }
      return obj;
    };

    Inputs.from_xml2 = function(xml, deferred, object_with_id) {
      var input, obj;
      if (!(xml != null) || xml.length === 0) {
        return null;
      }
      obj = new window.sirius.Inputs();
      input = xml.children('input');
      obj.set('input', _.map($(input), function(input_i) {
        return $a.Input.from_xml2($(input_i), deferred, object_with_id);
      }));
      if (obj.resolve_references) {
        obj.resolve_references(deferred, object_with_id);
      }
      return obj;
    };

    Inputs.prototype.to_xml = function(doc) {
      var xml;
      xml = doc.createElement('inputs');
      if (this.encode_references) {
        this.encode_references();
      }
      _.each(this.get('input') || [], function(a_input) {
        return xml.appendChild(a_input.to_xml(doc));
      });
      return xml;
    };

    Inputs.prototype.deep_copy = function() {
      return Inputs.from_xml1(this.to_xml(), {});
    };

    Inputs.prototype.inspect = function(depth, indent, orig_depth) {
      if (depth == null) {
        depth = 1;
      }
      if (indent == null) {
        indent = false;
      }
      if (orig_depth == null) {
        orig_depth = -1;
      }
      return null;
    };

    Inputs.prototype.make_tree = function() {
      return null;
    };

    return Inputs;

  })(Backbone.Model);

}).call(this);
