// Generated by CoffeeScript 1.3.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  window.sirius.IntersectionCache = (function(_super) {
    var $a;

    __extends(IntersectionCache, _super);

    /* $a = alias for sirius namespace
    */


    function IntersectionCache() {
      return IntersectionCache.__super__.constructor.apply(this, arguments);
    }

    $a = window.sirius;

    IntersectionCache.from_xml1 = function(xml, object_with_id) {
      var deferred, fn, obj, _i, _len;
      deferred = [];
      obj = this.from_xml2(xml, deferred, object_with_id);
      for (_i = 0, _len = deferred.length; _i < _len; _i++) {
        fn = deferred[_i];
        fn();
      }
      return obj;
    };

    IntersectionCache.from_xml2 = function(xml, deferred, object_with_id) {
      var IntersectionCacheEntry, obj;
      if (!(xml != null) || xml.length === 0) {
        return null;
      }
      obj = new window.sirius.IntersectionCache();
      IntersectionCacheEntry = xml.children('IntersectionCacheEntry');
      obj.set('intersectioncacheentry', _.map($(IntersectionCacheEntry), function(IntersectionCacheEntry_i) {
        return $a.IntersectionCacheEntry.from_xml2($(IntersectionCacheEntry_i), deferred, object_with_id);
      }));
      if (obj.resolve_references) {
        obj.resolve_references(deferred, object_with_id);
      }
      return obj;
    };

    IntersectionCache.prototype.to_xml = function(doc) {
      var xml;
      xml = doc.createElement('IntersectionCache');
      if (this.encode_references) {
        this.encode_references();
      }
      _.each(this.get('intersectioncacheentry') || [], function(a_intersectioncacheentry) {
        return xml.appendChild(a_intersectioncacheentry.to_xml(doc));
      });
      return xml;
    };

    IntersectionCache.prototype.deep_copy = function() {
      return IntersectionCache.from_xml1(this.to_xml(), {});
    };

    IntersectionCache.prototype.inspect = function(depth, indent, orig_depth) {
      if (depth == null) {
        depth = 1;
      }
      if (indent == null) {
        indent = false;
      }
      if (orig_depth == null) {
        orig_depth = -1;
      }
      return null;
    };

    IntersectionCache.prototype.make_tree = function() {
      return null;
    };

    return IntersectionCache;

  })(Backbone.Model);

}).call(this);
