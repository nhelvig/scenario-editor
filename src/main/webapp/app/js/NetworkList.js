// Generated by CoffeeScript 1.3.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  window.sirius.NetworkList = (function(_super) {
    var $a;

    __extends(NetworkList, _super);

    /* $a = alias for sirius namespace
    */


    function NetworkList() {
      return NetworkList.__super__.constructor.apply(this, arguments);
    }

    $a = window.sirius;

    NetworkList.from_xml1 = function(xml, object_with_id) {
      var deferred, fn, obj, _i, _len;
      deferred = [];
      obj = this.from_xml2(xml, deferred, object_with_id);
      for (_i = 0, _len = deferred.length; _i < _len; _i++) {
        fn = deferred[_i];
        fn();
      }
      return obj;
    };

    NetworkList.from_xml2 = function(xml, deferred, object_with_id) {
      var network, obj;
      if (!(xml != null) || xml.length === 0) {
        return null;
      }
      obj = new window.sirius.NetworkList();
      network = xml.children('network');
      obj.set('network', _.map($(network), function(network_i) {
        return $a.Network.from_xml2($(network_i), deferred, object_with_id);
      }));
      if (obj.resolve_references) {
        obj.resolve_references(deferred, object_with_id);
      }
      return obj;
    };

    NetworkList.prototype.to_xml = function(doc) {
      var xml;
      xml = doc.createElement('NetworkList');
      if (this.encode_references) {
        this.encode_references();
      }
      _.each(this.get('network') || [], function(a_network) {
        return xml.appendChild(a_network.to_xml(doc));
      });
      return xml;
    };

    NetworkList.prototype.deep_copy = function() {
      return NetworkList.from_xml1(this.to_xml(), {});
    };

    NetworkList.prototype.inspect = function(depth, indent, orig_depth) {
      if (depth == null) {
        depth = 1;
      }
      if (indent == null) {
        indent = false;
      }
      if (orig_depth == null) {
        orig_depth = -1;
      }
      return null;
    };

    NetworkList.prototype.make_tree = function() {
      return null;
    };

    return NetworkList;

  })(Backbone.Model);

}).call(this);
