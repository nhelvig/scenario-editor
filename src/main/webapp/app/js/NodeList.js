// Generated by CoffeeScript 1.3.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  window.sirius.NodeList = (function(_super) {
    var $a;

    __extends(NodeList, _super);

    /* $a = alias for sirius namespace
    */


    function NodeList() {
      return NodeList.__super__.constructor.apply(this, arguments);
    }

    $a = window.sirius;

    NodeList.from_xml1 = function(xml, object_with_id) {
      var deferred, fn, obj, _i, _len;
      deferred = [];
      obj = this.from_xml2(xml, deferred, object_with_id);
      for (_i = 0, _len = deferred.length; _i < _len; _i++) {
        fn = deferred[_i];
        fn();
      }
      return obj;
    };

    NodeList.from_xml2 = function(xml, deferred, object_with_id) {
      var node, obj;
      if (!(xml != null) || xml.length === 0) {
        return null;
      }
      obj = new window.sirius.NodeList();
      node = xml.children('node');
      obj.set('node', _.map($(node), function(node_i) {
        return $a.Node.from_xml2($(node_i), deferred, object_with_id);
      }));
      if (obj.resolve_references) {
        obj.resolve_references(deferred, object_with_id);
      }
      return obj;
    };

    NodeList.prototype.to_xml = function(doc) {
      var xml;
      xml = doc.createElement('NodeList');
      if (this.encode_references) {
        this.encode_references();
      }
      _.each(this.get('node') || [], function(a_node) {
        return xml.appendChild(a_node.to_xml(doc));
      });
      return xml;
    };

    NodeList.prototype.deep_copy = function() {
      return NodeList.from_xml1(this.to_xml(), {});
    };

    NodeList.prototype.inspect = function(depth, indent, orig_depth) {
      if (depth == null) {
        depth = 1;
      }
      if (indent == null) {
        indent = false;
      }
      if (orig_depth == null) {
        orig_depth = -1;
      }
      return null;
    };

    NodeList.prototype.make_tree = function() {
      return null;
    };

    return NodeList;

  })(Backbone.Model);

}).call(this);
