// Generated by CoffeeScript 1.3.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  window.sirius.PlanList = (function(_super) {
    var $a;

    __extends(PlanList, _super);

    /* $a = alias for sirius namespace
    */


    function PlanList() {
      return PlanList.__super__.constructor.apply(this, arguments);
    }

    $a = window.sirius;

    PlanList.from_xml1 = function(xml, object_with_id) {
      var deferred, fn, obj, _i, _len;
      deferred = [];
      obj = this.from_xml2(xml, deferred, object_with_id);
      for (_i = 0, _len = deferred.length; _i < _len; _i++) {
        fn = deferred[_i];
        fn();
      }
      return obj;
    };

    PlanList.from_xml2 = function(xml, deferred, object_with_id) {
      var obj, plan;
      if (!(xml != null) || xml.length === 0) {
        return null;
      }
      obj = new window.sirius.PlanList();
      plan = xml.children('plan');
      obj.set('plan', _.map($(plan), function(plan_i) {
        return $a.Plan.from_xml2($(plan_i), deferred, object_with_id);
      }));
      if (obj.resolve_references) {
        obj.resolve_references(deferred, object_with_id);
      }
      return obj;
    };

    PlanList.prototype.to_xml = function(doc) {
      var xml;
      xml = doc.createElement('PlanList');
      if (this.encode_references) {
        this.encode_references();
      }
      _.each(this.get('plan') || [], function(a_plan) {
        return xml.appendChild(a_plan.to_xml(doc));
      });
      return xml;
    };

    PlanList.prototype.deep_copy = function() {
      return PlanList.from_xml1(this.to_xml(), {});
    };

    PlanList.prototype.inspect = function(depth, indent, orig_depth) {
      if (depth == null) {
        depth = 1;
      }
      if (indent == null) {
        indent = false;
      }
      if (orig_depth == null) {
        orig_depth = -1;
      }
      return null;
    };

    PlanList.prototype.make_tree = function() {
      return null;
    };

    return PlanList;

  })(Backbone.Model);

}).call(this);
